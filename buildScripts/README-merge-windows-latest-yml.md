# Merge Windows latest.yml Script

## Purpose

This script generates a properly formatted `latest.yml` file containing both x64 and ARM64 Windows installers for electron-updater auto-update functionality.

Use this when you need to manually fix a GitHub release's `latest.yml` file without re-running the entire build pipeline.

## The Problem It Solves

When Windows x64 and ARM64 builds upload separate `latest.yml` files, one overwrites the other. This causes electron-updater to only see one architecture, leading to users downloading the wrong installer.

The merged `latest.yml` contains both architectures, allowing electron-updater to automatically select the correct one based on the user's system architecture.

## Prerequisites

- `bash` shell (Linux, macOS, WSL, or Git Bash on Windows)
- `curl` (for downloading from GitHub releases)
- `sha256sum` and `sha512sum` (for hash calculation)
- `gh` CLI (optional, for uploading to GitHub)

## Usage

### Option 1: Download from Existing GitHub Release

Use this to fix an already-published release (like v1.1.3):

```bash
cd buildScripts
./merge-windows-latest-yml.sh 1.1.3 v1.1.3
```

This will:
1. Download both x64 and ARM64 installers from the v1.1.3 GitHub release
2. Calculate SHA256/SHA512 hashes
3. Generate a merged `latest.yml` in the current directory

### Option 2: Use Local Build Files

Use this when you have local build artifacts:

```bash
cd buildScripts
./merge-windows-latest-yml.sh 1.1.3
```

This will look for EXE files in the `build/` directory.

## Output

The script creates a `latest.yml` file in the current directory:

```yaml
version: 1.1.3
files:
  - url: Power-Platform-ToolBox-1.1.3-x64-win.exe
    sha512: <calculated-x64-sha512>
    sha256: <calculated-x64-sha256>
    size: 83575696
    blockMapSize: null
  - url: Power-Platform-ToolBox-1.1.3-arm64-win.exe
    sha512: <calculated-arm64-sha512>
    sha256: <calculated-arm64-sha256>
    size: 86587224
    blockMapSize: null
releaseDate: 2026-02-19T10:54:00.000Z
```

## Uploading to GitHub Release

### Using GitHub CLI

```bash
gh release upload v1.1.3 latest.yml --clobber
```

The `--clobber` flag replaces the existing `latest.yml` file.

### Using GitHub Web UI

1. Go to https://github.com/PowerPlatformToolBox/desktop-app/releases/edit/v1.1.3
2. Scroll to the release assets section
3. Delete the existing `latest.yml` file
4. Upload the new `latest.yml` file generated by this script

## Fixing v1.1.3 Release

To fix the current v1.1.3 release:

```bash
cd buildScripts
./merge-windows-latest-yml.sh 1.1.3 v1.1.3
gh release upload v1.1.3 latest.yml --clobber
```

After uploading, users who check for updates will receive the correct installer for their architecture.

## How It Works

1. **Locates Installers**: Finds or downloads both x64 and ARM64 Windows EXE files
2. **Calculates Hashes**: Computes SHA256 and SHA512 checksums for integrity verification
3. **Generates YAML**: Creates a YAML file with both file entries
4. **Removes Individual Files**: The old approach of separate YAMLs is replaced

## Architecture Detection

The electron-updater library automatically detects the user's architecture:

- On x64 systems: `process.arch === "x64"` → downloads `*-x64-win.exe`
- On ARM64 systems: `process.arch === "arm64"` → downloads `*-arm64-win.exe`

This is done by matching the architecture string in the filename.

## Troubleshooting

### "Failed to download x64 installer"

The release tag or version doesn't exist on GitHub. Check:
- The release tag is correct (e.g., `v1.1.3` not `1.1.3`)
- The release has been published (not a draft)
- The installers have been uploaded to the release

### "Could not find x64 installer in build/ directory"

When using local files, ensure you've run the build first:

```bash
pnpm run build
pnpm run package:win       # For x64
pnpm run package:win-arm64 # For ARM64
```

### "sha256sum: command not found"

On macOS, you may need to install coreutils:

```bash
brew install coreutils
```

Then use `gsha256sum` instead of `sha256sum` in the script.

## Related Files

- `.github/workflows/prod-release.yml` - Production release workflow (includes automated merge step)
- `.github/workflows/nightly-release.yml` - Nightly release workflow (includes automated merge step)

## Future Enhancements

Starting with v1.1.4 and later, the GitHub Actions workflows automatically merge the Windows YAML files, so manual intervention won't be needed for new releases.
